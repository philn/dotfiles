#!/bin/sh
VERSION={{- .chezmoi.osRelease.versionID }}
podman start fedora-toolbox-${VERSION}
exec podman exec -i -e PATH=$HOME/.cargo-home/bin:$PATH -u phil fedora-toolbox-${VERSION} $HOME/.cargo-home/bin/rust-analyzer "$@"
